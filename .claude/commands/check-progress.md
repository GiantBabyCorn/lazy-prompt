Check the current development progress of the lazy-prompt project.

1. Read CLAUDE.md to see the progress checklist
2. Run `git log --oneline -15` to see recent commits
3. Run `npm run validate` to check data integrity (if data files exist)
4. Report:
   - Which tasks are complete
   - Which task is currently in progress
   - What needs to be done next
   - Any issues or blockers found
